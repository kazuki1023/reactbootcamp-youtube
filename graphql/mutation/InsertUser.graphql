mutation InsertUser($id: String!, $name: String!, $email: String!) {
  insert_users_one(object: {id: $id, name: $name, profile_photo_url: "", email: $email}) {
    id
    name
    email
    profile_photo_url
    created_at
    updated_at
  }
}
